// ClientConnectionDomain — the contracts the phone talks to a Mac through: the repository
// protocols every other Client feature depends on, the pairing model, and the typed errors the
// API contract defines.
//
// Milestone M4.
