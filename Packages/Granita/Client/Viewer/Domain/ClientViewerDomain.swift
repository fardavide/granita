// ClientViewerDomain — the policies the diff viewer runs on: which hunks are expanded, which
// files are viewed, how wrap mode maps to row counts, and how far ahead to prefetch.
//
// Milestone M5.
