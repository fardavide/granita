// ServerGitData — ProcessGitClient: the git binary over swift-subprocess, hardened against a
// developer's own global git configuration, draining stdout and stderr concurrently.
//
// Milestone M2.
