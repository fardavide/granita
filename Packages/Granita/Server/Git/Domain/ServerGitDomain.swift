// ServerGitDomain — the GitClient protocol every git invocation goes through, and the typed
// errors it fails with. The protocol is what keeps a libgit2 backend possible without touching a
// call site, and what keeps a Mac App Store build on the table.
//
// Milestone M2.
