// ServerSessionsData — indexing Claude Code session transcripts into suggested worktree
// aliases. Best effort, never blocking a request, and never carrying more than 60 characters of
// conversation text onto the network.
//
// Milestone M2.
