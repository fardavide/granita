// ServerApiPresentation — the HTTP surface: Hummingbird routes, TLS, bearer auth, and the
// event stream. This is the server's presentation layer in the same sense as the client's:
// domain-to-wire mapping plus routes, with no views to have a Ui sibling for.
//
// Milestone M2.
