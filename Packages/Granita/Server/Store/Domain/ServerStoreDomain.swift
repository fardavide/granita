// ServerStoreDomain — the Store protocol and the records it holds. One conformer in v1; the
// protocol is here so v2 can swap the implementation when comments make the data grow.
//
// Milestone M2.
